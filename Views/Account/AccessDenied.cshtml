@{
    ViewData["Title"] = "Access Denied";
}

<div class="login-container">
    <div class="login-card">
        <div style="text-align: center;">
            <div style="font-size: 64px; margin-bottom: 20px;">🚫</div>
            <h2 style="color: #f44336; margin-bottom: 20px;">Access Denied</h2>
            <p style="color: var(--dusty-rose); margin-bottom: 30px;">You do not have permission to access this resource.</p>
            <a href="@Url.Action("Index", "Home")" class="btn btn-primary"><span>Return to Home</span></a>
        </div>
    </div>
</div>
